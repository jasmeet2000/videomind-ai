# =============================================================================
# VideoMind AI — Python Dependencies
# Pin major versions; exact pins added after lock-file generation.
# =============================================================================

# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9        # File upload support for FastAPI

# --- Data validation ---
pydantic>=2.7.0
pydantic-settings>=2.3.0       # BaseSettings from env vars (12-Factor)

# --- Logging ---
loguru>=0.7.2

# --- Database ---
asyncpg>=0.29.0                # Async PostgreSQL driver
sqlalchemy[asyncio]>=2.0.30    # ORM + async support

# --- Vector database ---
qdrant-client>=1.9.0

# --- Video / Audio ---
opencv-python-headless>=4.9.0  # Frame extraction (headless = no GUI deps)
ffmpeg-python>=0.2.0           # FFmpeg wrapper

# --- Speech-to-Text ---
openai-whisper>=20231117
faster-whisper>=1.0.0          # Distil-Whisper / optimized inference

# --- OCR ---
paddleocr>=2.7.3
easyocr>=1.7.1

# --- Vision / Object Detection ---
# ultralytics>=8.2.0           # YOLOv8 — uncomment in Phase 5
torch>=2.3.0                   # Required by Whisper, EasyOCR, YOLO
torchvision>=0.18.0

# --- Embeddings ---
sentence-transformers>=3.0.0   # BAAI/bge-small-en-v1.5
transformers>=4.41.0

# --- Retrieval ---
rank-bm25>=0.2.2               # BM25 sparse retrieval

# --- LLM / Ollama ---
ollama>=0.2.0                  # Official Ollama Python client
httpx>=0.27.0                  # Async HTTP client (used by Ollama + health checks)

# --- Utilities ---
python-dotenv>=1.0.1
numpy>=1.26.0
Pillow>=10.3.0                 # Image handling
tqdm>=4.66.0                   # Progress bars in pipelines

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0                  # For FastAPI TestClient

# --- Code quality ---
ruff>=0.4.0                    # Linter + formatter
isort>=5.13.0                  # Import sorting
mypy>=1.10.0                   # Static type checking
